package com.ggetters.app.ui.central.viewmodels

import androidx.lifecycle.ViewModel
import dagger.hilt.android.lifecycle.HiltViewModel
import javax.inject.Inject

@HiltViewModel
class MatchControlViewModel @Inject constructor() : ViewModel() {
    
    // TODO: Backend - Add repository injection for match data
    // private val matchRepository: MatchRepository
    
    // TODO: Backend - Add LiveData for match state
    // private val _matchState = MutableLiveData<MatchState>()
    // val matchState: LiveData<MatchState> = _matchState
    
    // TODO: Backend - Add methods for match control
    // fun startMatch(matchId: String)
    // fun pauseMatch(matchId: String)
    // fun endMatch(matchId: String)
    // fun recordEvent(event: MatchEvent)
    // fun undoLastEvent(matchId: String)
} 
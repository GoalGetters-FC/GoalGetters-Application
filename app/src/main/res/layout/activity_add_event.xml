<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#161620">

    <LinearLayout
        android:orientation="vertical"
        android:layout_height="match_parent"
        android:layout_width="match_parent">

        <!-- Header Section (matches account tab) -->
        <LinearLayout
            android:orientation="vertical"
            android:paddingTop="16dp"
            android:paddingBottom="32dp"
            android:paddingHorizontal="28dp"
            android:layout_height="wrap_content"
            android:layout_width="match_parent">

            <!-- Close Button -->
            <ImageButton
                android:id="@+id/closeButton"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:src="@drawable/ic_unicons_close_24"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="Close"
                app:tint="#FFFFFF"
                android:layout_gravity="start"
                android:layout_marginBottom="24dp" />

            <!-- Title -->
            <TextView
                android:text="Schedule Event"
                android:textColor="#FFFFFF"
                android:textSize="24sp"
                android:textFontWeight="600"
                android:fontFamily="@font/variable_inter"
                android:layout_height="wrap_content"
                android:layout_width="match_parent"
                android:layout_marginBottom="8dp" />

            <!-- Subtitle -->
            <TextView
                android:text="Create a new team event"
                android:textColor="#FFFFFF"
                android:textSize="16sp"
                android:textFontWeight="500"
                android:fontFamily="@font/variable_inter"
                android:layout_height="wrap_content"
                android:layout_width="match_parent" />

        </LinearLayout>

        <!-- Content Section (matches account tab) -->
        <com.google.android.material.card.MaterialCardView
            app:contentPadding="0dp"
            app:contentPaddingTop="16dp"
            app:cardBackgroundColor="#FFFFFF"
            app:cardPreventCornerOverlap="false"
            app:shapeAppearanceOverlay="@style/ShapeAppearance.TopCorner40"
            app:strokeWidth="0dp"
            app:cardElevation="6dp"
            android:elevation="6dp"
            android:layout_height="match_parent"
            android:layout_width="match_parent">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:orientation="vertical">

                <!-- Tab Layout -->
                <com.google.android.material.tabs.TabLayout
                    android:id="@+id/eventTypeTabLayout"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:background="@color/white"
                    android:layout_marginHorizontal="20dp"
                    android:layout_marginTop="8dp"
                    app:tabMode="fixed"
                    app:tabGravity="fill"
                    app:tabIndicatorColor="@color/primary"
                    app:tabSelectedTextColor="@color/primary"
                    app:tabTextColor="@color/text_secondary"
                    app:tabTextAppearance="@style/TabTextAppearance"
                    app:tabIndicatorHeight="3dp"
                    app:tabIndicatorGravity="bottom">

                    <com.google.android.material.tabs.TabItem
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="Game"
                        app:icon="@drawable/ic_game_modern_24" />

                    <com.google.android.material.tabs.TabItem
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="Practice"
                        app:icon="@drawable/ic_practice_modern_24" />

                    <com.google.android.material.tabs.TabItem
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="Event"
                        app:icon="@drawable/ic_unicons_calendar_24" />

                </com.google.android.material.tabs.TabLayout>

                <!-- Content Area -->
                <androidx.viewpager2.widget.ViewPager2
                    android:id="@+id/eventTypeViewPager"
                    android:layout_width="match_parent"
                    android:layout_height="0dp"
                    android:layout_weight="1" />

                <!-- Schedule Button -->
                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical"
                    android:padding="20dp"
                    android:background="@color/white">

                    <com.google.android.material.button.MaterialButton
                        android:id="@+id/scheduleButton"
                        android:layout_width="match_parent"
                        android:layout_height="56dp"
                        android:text="Schedule Event"
                        android:textColor="@color/white"
                        android:textSize="16sp"
                        android:textFontWeight="600"
                        android:fontFamily="@font/variable_inter"
                        app:backgroundTint="@color/primary"
                        app:cornerRadius="28dp"
                        app:icon="@drawable/ic_unicons_calendar_24"
                        app:iconTint="@color/white"
                        style="@style/Widget.MaterialComponents.Button" />

                </LinearLayout>

            </LinearLayout>

        </com.google.android.material.card.MaterialCardView>
    </LinearLayout>
</androidx.coordinatorlayout.widget.CoordinatorLayout> 
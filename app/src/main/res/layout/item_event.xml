<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginVertical="4dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="2dp"
    app:cardBackgroundColor="@color/white"
    android:foreground="?attr/selectableItemBackground"
    android:clickable="true"
    android:focusable="true">

    <LinearLayout
        android:id="@+id/eventContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="16dp"
        android:gravity="center_vertical">

        <!-- Event Type Icon -->
        <TextView
            android:id="@+id/eventTypeIcon"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:text="🔵"
            android:textSize="24sp"
            android:gravity="center"
            android:background="@drawable/event_icon_background"
            android:layout_marginEnd="16dp" />

        <!-- Event Details -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/eventTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Team Practice"
                android:textSize="16sp"
                android:textStyle="bold"
                android:textColor="@color/black"
                android:fontFamily="@font/variable_inter"
                android:layout_marginBottom="8dp"
                android:maxLines="1"
                android:ellipsize="end" />

            <!-- Time and Venue Row -->
            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:layout_marginBottom="6dp">

                <TextView
                    android:id="@+id/eventTime"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="15:00"
                    android:textSize="14sp"
                    android:textColor="@color/text_secondary"
                    android:fontFamily="@font/variable_inter"
                    android:textStyle="bold"
                    android:layout_marginEnd="8dp" />

                <View
                    android:layout_width="1dp"
                    android:layout_height="match_parent"
                    android:background="@color/divider"
                    android:layout_marginEnd="8dp" />

                <TextView
                    android:id="@+id/eventVenue"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Main Field"
                    android:textSize="14sp"
                    android:textColor="@color/text_secondary"
                    android:fontFamily="@font/variable_inter"
                    android:maxLines="1"
                    android:ellipsize="end" />

            </LinearLayout>

            <!-- Opponent (for matches) -->
            <TextView
                android:id="@+id/eventOpponent"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="vs Eagles FC"
                android:textSize="14sp"
                android:textColor="#FFD400"
                android:textStyle="bold"
                android:fontFamily="@font/variable_inter"
                android:visibility="gone"
                android:drawablePadding="6dp"
                android:gravity="center_vertical"
                app:drawableStartCompat="@drawable/ic_unicons_soccer_24"
                app:drawableTint="#FFD400" />

        </LinearLayout>

        <!-- Event Type Badge -->
        <TextView
            android:id="@+id/eventTypeBadge"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Practice"
            android:textSize="12sp"
            android:textColor="@color/text_secondary"
            android:background="@drawable/status_badge_background"
            android:paddingHorizontal="10dp"
            android:paddingVertical="6dp"
            android:layout_marginStart="12dp"
            android:fontFamily="@font/variable_inter"
            android:textStyle="bold" />

    </LinearLayout>

</androidx.cardview.widget.CardView> 